










let soup = {
    a: 'chicken noodles',
    b: 'tomato',
    c: 'beef and barley'
};
/* 
objects are key value pairs, which just means in an object there are keys(what the
    name of this part of the object is),
    and a value for that key

    */
console.log(soup.a)
// This is called dot notation for an object. This is how we can dig through an object
// to get back data we want

let car = "Honda",

let dec = 10.2;